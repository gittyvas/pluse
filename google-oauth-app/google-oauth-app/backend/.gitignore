    # Logs
    logs/
    *.log
    npm-debug.log*
    yarn-debug.log*
    pnpm-debug.log*
    lerna-debug.log*

    # Dependency directories
    node_modules/

    # Environment variables
    .env
    .env.local
    .env.*.local

    # IDEs and OS specific
    .idea/
    .vscode/
    *.iml
    .DS_Store
    Thumbs.db

    # Windows Zone.Identifier files
    *:Zone.Identifier

    # Backup files
    *.bak
    *.bak-*
    *~
    *.tmp
    *.swp # Vim swap files

    # Other temporary/generated files
    /tmp/

    # If you are using the FIREBASE_CONFIG environment variable,
    # you should NOT track your actual serviceAccountKey.json file directly.
    # If you are NOT using the env var and are reading the file directly,
    # then you *do* need to track it.
    # If you switch to env var, uncomment the line below:
    # serviceAccountKey.json
    # pulse-crm-60584-firebase-adminsdk-fbsvc-f3c8a6ba53.json
    
