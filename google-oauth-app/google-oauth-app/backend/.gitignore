    # Logs
    logs/
    *.log
    npm-debug.log*
    yarn-debug.log*
    pnpm-debug.log*
    lerna-debug.log*

    # Dependency directories
    node_modules/

    # Environment variables
    .env
    .env.local
    .env.*.local

    # IDEs and OS specific
    .idea/
    .vscode/
    *.iml
    .DS_Store
    Thumbs.db

    # Windows Zone.Identifier files
    *:Zone.Identifier

    # Backup files
    *.bak
    *.bak-*
    *~
    *.tmp
    *.swp # Vim swap files

    # Firebase Admin SDK service account key
    # IMPORTANT: If you are loading FIREBASE_CONFIG from an environment variable,
    # then you should uncomment the lines below to IGNORE your serviceAccountKey.json file.
    # If you are still loading it directly from the file, keep these commented out.
    # serviceAccountKey.json
    # pulse-crm-60584-firebase-adminsdk-fbsvc-f3c8a6ba53.json
        
